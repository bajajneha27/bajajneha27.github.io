# Transformer

A Transformer is a type of deep learning architecture. Before Transformers, models read text like a person—one word at a time from left to right. The Transformer changed this by using a mechanism called *Self-Attention*, which allows it to look at every word in a sentence simultaneously to understand the relationships between them.

### Functional Blocks

- **Encoder**: This part of the model is responsible for "understanding" the input. It takes a sentence and converts it into Dense Vector or Embedding.

- **Decoder**: This part takes the abstract meaning created by the encoder and uses it to generate new text, one word at a time. It’s what allows AI to "talk" back to you.